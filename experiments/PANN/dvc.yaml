stages:
  train:
    cmd: python main.py --train --root_path ../../ --model_path model.pt
    deps:
    - main.py
    - trainer.py
    - model.py  
    params:
      - train.random_seed
      - train.nepochs
      - train.learning_rate
      - train.batch_size
      - features.sampling_rate
      - features.global_normalization
    outs:  
    - model.pt
    - traced_model.pt
    live:
      training_metrics:
        summary: true
        html: true
