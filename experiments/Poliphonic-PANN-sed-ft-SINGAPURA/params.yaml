features:
  sampling_rate: 32000
  overwrite: false
  number_of_channels: 1 # 1 / 2 ,  (mono / stereo)
  combine_channels: 'mean' # 'left' / 'right' / 'mean'
  collate_resize: 'crop' # 'pad' / 'crop' / 'none'
  waveform_normalization:
    scope: none
    type: none

train:
  random_seed: 54321 # int
  train_percent: 0.7 # float [0,1]
  nepochs: 100 # int
  learning_rate: 0.00001 # float
  batch_size: 32 # int
  model: 'PANN-sed'
  dataset: ['SINGAPURA'] #SINGAPURA AUMILAB
  ft_percent: 0.9
  stopping_criteria: 'valid_loss'
  patience: 15
  finetuning: 'SPASS' #None, 'SPASS', 'PANN-pretrained'
  freeze: False # True, False
  clf_head: '1L' #None, '1L', '3L'
  
evaluate:
  dataset: ['Poliphonic']
